Fixed: the universal APK on GitHub shipped incomplete x86/x86_64 libraries, so it
crashed on startup on those devices and in emulators. The build now packages only
architectures it actually compiles, and an x86_64 build is published as well.
